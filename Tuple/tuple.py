# Immutable: once created, elements cannot be 
# added, removed or edited form the tuple.

# There is no need of braces, by default without braces ele,emts would be considered as tuples.

marks = (99,99, 98, 97, 100)

print(marks.count(99))
print(marks.index(99))  #returns index value of first appearing value